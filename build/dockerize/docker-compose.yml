version: "3.9"

services:
    denodocker:
        container_name: denodocker
        image: denodocker
        restart: always
        build:
            context: .
            dockerfile: Dockerfile
            target: base
        ports:
            - "${PORT}:${PORT}"
